// Formulario Serway

#import "config.typ": conf
#import "shortcuts.typ": vv, norm
#show: conf

#align(center)[
  #stack(
    dir: ttb,
    spacing: 0.35em,
    text(size: 10pt)[Formularios de ],
    text(size: 10pt)[Serway],
  )
]
#line(length: 100%, stroke: 0.6pt)
#v(0.3em)

#columns(2)[
]

#v(0.4em)
